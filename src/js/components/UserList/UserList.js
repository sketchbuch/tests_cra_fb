// @flow

import React, { Component } from 'react';
import './UserList.css';

type Props = {
};

/**
* User List component for displaying blog users.
*/
export class UserList extends Component<Props> {
  props: Props;

  render() {
    return (
      <div className="UserList">
        user list
      </div>
    );
  }
}


export default UserList;
